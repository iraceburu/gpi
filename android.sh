# Accedemos a la carpeta del proyecto
cd webview-master
# Vemos todas las tasks del graddle
./gradlew tasks
# Task de compilación y empaquetado
./gradlew assembleDebug
# Task de ejecución de tests
./gradlew test